<h1>This is the Tags Page</h1>

<%= link_to "New Tag", new_tag_path %>

<ul class="tags-list">
    <% @tags.each do |tag| %>
        <li class="tag-item">
            <%= link_to tag.title, tag %>
        </li>
    <% end %>
</ul>